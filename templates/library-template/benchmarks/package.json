{
  "name": "library-name-benchmarks",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "bench": "node --import tsx index.bench.ts",
    "bench:compare": "node --import tsx index.bench.ts && echo 'Add competitor benchmarks here'"
  },
  "devDependencies": {
    "tatami-ng": "^0.8.18",
    "tsx": "^4.7.0"
  }
}
